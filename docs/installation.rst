============
Installation
============

At the command line::

    $ easy_install python-livefyre

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv python-livefyre
    $ pip install python-livefyre